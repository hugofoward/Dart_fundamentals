int Function(int) sumPartial(int a) {
  int c = 0;

/* 
  Instructions below are executed only when the second parameter is specified
  Instruções abaixo são executadas apenas quando o segundo parâmetro é especificado
*/
  return (int b) {
    return a + b + c;
  };
}

main() {
/*
  The second internal parameter (b) is specified in the second position ()
  O segundo parâmetro interno (b) é especificado na segunda posição ()
*/
  print(sumPartial(2)(10));

  var sumWith10 = sumPartial(10);

  print(sumWith10(3));
}
